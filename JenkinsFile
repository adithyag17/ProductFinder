pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/adithyag17/ProductFinder'
            }
        }
        stage('Build and Run Containers') {
            steps {
                script {
                    sh 'docker-compose up -d'
                }
            }
        }
    }
}
